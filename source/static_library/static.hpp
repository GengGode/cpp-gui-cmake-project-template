#pragma once
#include <global_regerr.hpp>

struct texture
{
    // Texture data and methods would go here
};

// struct debugger
// {
//     void watch(std::string_view name, std::shared_ptr<texture> tex);
//     template <typename T> void watch(std::string_view name, T number);
//     template <typename T> void watch_append(std::string_view name, T number);
// };